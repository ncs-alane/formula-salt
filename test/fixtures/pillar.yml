salt:
  minion:
    config:
      master: localhost
  master:
    config:
      auto_accept: true
      open_mode: true
      fileserver_backend:
        - git
      gitfs_remotes:
        - https://github.com/mulesoft/formula-salt
