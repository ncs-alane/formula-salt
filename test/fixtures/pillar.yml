salt:
  version: 2015.8.8-2.el6
  minion:
    config:
      master: localhost
  master:
    config:
      auto_accept: true
      open_mode: true
      fileserver_backend:
        - git
      gitfs_remotes:
        - https://github.com/mulesoft/formula-salt
